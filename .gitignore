# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Environment variables
.env
.env.*
!.env.example

# PayPal secrets — real credentials never go in git (see PAYPAL-SETUP.md)
public/api/paypal/credentials.local.php
novelio-paypal-credentials.php

# Payoneer secrets — real credentials never go in git (see PAYONEER-SETUP.md)
public/api/payoneer/credentials.local.php
novelio-payoneer-credentials.php

# Admin panel secrets — DB + GitHub PAT never go in git (see ADMIN-SETUP.md)
public/api/admin/credentials.local.php
# Any other filled-in copy. The .example file is tracked and must only ever
# contain placeholders — a real token was pasted into it once.
public/api/admin/credentials.real.php
public/api/admin/novelio-admin-credentials.php
novelio-admin-credentials.php

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Admin panel local test data (real data lives above the web root on the server)
novelio-admin-data/
